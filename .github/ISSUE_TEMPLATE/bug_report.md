---
name: Bug report
about: Something in the Free CLI (or another tier) isn't behaving correctly
title: "[bug] "
labels: bug
---

**Tier** (Free CLI / Enterprise runner / Pro service):

**`apidrift` version** (`apidrift -v`):

**Command run**

```bash

```

**Expected behavior**

**Actual behavior**

**Target repo shape** (language, framework, which vendor/API pattern) — no
need to share private code, a minimal repro is ideal:

**Inference mode** (deterministic-only / BYOT with `--ai` / managed):

**Logs / output**

```

```

**Does this affect the trust guarantees?** (i.e. did a PR open on a red
test suite, or did a test file get modified?) If yes, please report it
privately instead — see [SECURITY.md](../../SECURITY.md).
